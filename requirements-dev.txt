# Development dependencies for study-finder
# Installs the runtime deps plus tooling for testing, linting and notebooks.
# Install with: pip install -r requirements-dev.txt

-r requirements.txt

# Testing
pytest>=8.0
responses>=0.25          # mock HTTP responses in tests

# Linting / formatting
ruff>=0.4

# Exploration
jupyter>=1.0
